# راهنمای ساده و کامل پروژه Gadget

این فایل برای زمانی است که می‌خواهید بدون درگیرشدن با جزئیات پیچیده، بفهمید پروژه چه کاری انجام می‌دهد، چگونه روی سرور Linux اجرا می‌شود و برای هر کار معمول باید چه اقدامی انجام دهید.

راهنمای فنی و قراردادهای دقیق JSON در [docs/راهنمای جامع سرور و یکپارچه‌سازی](docs/%D8%B1%D8%A7%D9%87%D9%86%D9%85%D8%A7%DB%8C-%D8%AC%D8%A7%D9%85%D8%B9-%D8%B3%D8%B1%D9%88%D8%B1-%D9%88-%DB%8C%DA%A9%D9%BE%D8%A7%D8%B1%DA%86%D9%87%E2%80%8C%D8%B3%D8%A7%D8%B2%DB%8C.md) قرار دارد. این فایل نسخهٔ ساده‌تر و عملیاتی آن است.

---

## 1. این پروژه دقیقاً چیست؟

این پروژه سرور مرکزی دستگاه‌های نمایشگر ESP32 است.

فرض کنید یک نمایشگر در فروشگاه، شرکت یا خانه نصب شده است. آن نمایشگر برای کارهای زیر به سرور وصل می‌شود:

1. بفهمد چه چیزی روی صفحه نشان دهد.
2. تنظیمات جدید را دریافت کند.
3. هر چند دقیقه بگوید روشن و آنلاین است.
4. درصد باتری، قدرت Wi‑Fi و نسخهٔ نرم‌افزارش را گزارش کند.
5. اگر نسخهٔ جدید firmware وجود داشت، آن را دریافت و نصب کند.

اطلاعات مشتری و اشتراک معمولاً در WordPress نگهداری می‌شود. سرور Django این اطلاعات را از WordPress می‌گیرد، برای دستگاه قابل استفاده می‌کند و نتیجه را در دیتابیس خود نگه می‌دارد.

```text
WordPress
   │ اطلاعات مشتری، plan و تنظیمات
   ▼
سرور Django
   │ config، status و firmware
   ▼
ESP32 Display
```

---

## 2. اجزای پروژه به زبان ساده

| بخش | وظیفهٔ ساده |
|---|---|
| `api` | سرور اصلی Django؛ درخواست‌های دستگاه و WordPress را می‌گیرد |
| `db` | دیتابیس PostgreSQL؛ اطلاعات مهم را دائمی نگه می‌دارد |
| `redis` | حافظه و صف سریع برای کارهای پس‌زمینه |
| `worker` | کارهای زمان‌بر را خارج از API انجام می‌دهد |
| `beat` | کارهای دوره‌ای را زمان‌بندی می‌کند |
| `mqtt` | پیام لحظه‌ای برای دستگاه‌ها می‌فرستد |
| `Nginx` | در Linux جلوی API قرار می‌گیرد و HTTPS را مدیریت می‌کند |
| `WordPress` | منبع اطلاعات مشتری، اشتراک و تنظیمات تجاری |

### یک مثال واقعی

1. مدیر در WordPress رنگ صفحهٔ دستگاه را تغییر می‌دهد.
2. WordPress به Django پیام می‌دهد.
3. Django تنظیمات جدید را ذخیره می‌کند و شمارهٔ نسخهٔ UI را افزایش می‌دهد.
4. Django از طریق MQTT به ESP32 می‌گوید: «تنظیمات جدید داری».
5. ESP32 از API تنظیمات جدید را می‌گیرد.
6. ESP32 تنظیمات را روی نمایشگر اجرا می‌کند.
7. ESP32 در heartbeat بعدی می‌گوید نسخهٔ جدید تنظیمات را اعمال کرده است.

---

## 3. فایل‌ها و پوشه‌های مهم

| مسیر | کاربرد |
|---|---|
| [backend](backend) | کل کد Django و API |
| [backend/core/models.py](backend/core/models.py) | شکل جدول‌ها و اطلاعات اصلی سیستم |
| [backend/core/views.py](backend/core/views.py) | APIهایی که WordPress و دستگاه صدا می‌زنند |
| [backend/core/services.py](backend/core/services.py) | منطق sync WordPress و ساخت config دستگاه |
| [backend/core/tasks.py](backend/core/tasks.py) | کارهای پس‌زمینه و زمان‌بندی‌شده |
| [backend/core/admin.py](backend/core/admin.py) | پنل مدیریت Django Admin |
| [backend/core/mqtt.py](backend/core/mqtt.py) | ارسال پیام MQTT به دستگاه |
| [docker-compose.yml](docker-compose.yml) | اجرای local برای توسعه |
| [deploy/linux](deploy/linux) | فایل‌های مخصوص سرور Linux production |
| [.env.example](.env.example) | نمونهٔ تنظیمات و secretهای لازم |
| [Makefile](Makefile) | دستورهای کوتاه برای کار روزمره |
| [docs](docs) | مستندات کامل‌تر |

---

## 4. قبل از اجرا روی سرور Linux چه چیزهایی لازم است؟

برای سرور واقعی پیشنهاد می‌شود از Ubuntu 22.04 یا Ubuntu 24.04 استفاده کنید.

نیازها:

- یک VPS یا سرور Linux با حداقل 2 CPU و 4GB RAM
- یک domain مثل `api.example.com`
- یک subdomain برای MQTT مثل `mqtt.example.com`
- DNS هر دو domain به IP سرور اشاره کند
- Docker Engine
- Nginx
- Certbot برای HTTPS رایگان Let's Encrypt
- دسترسی SSH با key، نه password

### پورت‌هایی که از اینترنت باید باز باشند

| پورت | کاربرد |
|---:|---|
| `22` | SSH برای مدیریت سرور |
| `80` | دریافت اولیهٔ certificate و redirect HTTP |
| `443` | سایت/API امن HTTPS |
| `8883` | MQTT امن برای ESP32 |

پورت‌های `5432`، `6379` و `8000` نباید عمومی باشند.

---

## 5. نصب اولیه روی Linux؛ مرحله به مرحله

### مرحله 1: اتصال به سرور

```bash
ssh your-user@SERVER_IP
```

### مرحله 2: نصب ابزارهای پایه

```bash
sudo apt update && sudo apt -y upgrade
sudo apt install -y ca-certificates curl git make nginx certbot python3-certbot-nginx ufw
```

### مرحله 3: نصب Docker

```bash
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker "$USER"
newgrp docker
```

بعد از این کار، در صورت نیاز یک بار از SSH خارج و دوباره وصل شوید.

### مرحله 4: فعال‌کردن firewall

```bash
sudo ufw allow OpenSSH
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 8883/tcp
sudo ufw enable
```

### مرحله 5: دریافت پروژه

```bash
sudo mkdir -p /opt/gadget
sudo chown "$USER":"$USER" /opt/gadget
git clone https://github.com/VahidMB/New-Gadget-Project.git /opt/gadget/app
cd /opt/gadget/app
```

اگر repository خصوصی است، ابتدا SSH key یا access token GitHub را روی سرور تنظیم کنید.

### مرحله 6: ساخت تنظیمات production

```bash
cp .env.example .env.production
chmod 600 .env.production
nano .env.production
```

مقادیر مهم را تغییر دهید:

```dotenv
DJANGO_DEBUG=0
DJANGO_SECURE_SSL_REDIRECT=1
DJANGO_ALLOWED_HOSTS=api.example.com

DJANGO_SECRET_KEY=یک-secret-طولانی-و-تصادفی
POSTGRES_PASSWORD=یک-password-قوی

WORDPRESS_WEBHOOK_SECRET=یک-secret-مشترک-با-wordpress
WORDPRESS_SYNC_TRIGGER_TOKEN=یک-token-طولانی
METRICS_TOKEN=یک-token-طولانی

MQTT_USERNAME=نام-کاربری-mqtt
MQTT_PASSWORD=password-قوی-mqtt
MQTT_USE_TLS=1
MQTT_PORT=8883
MQTT_CERTS_DIR=/etc/letsencrypt/live/mqtt.example.com
MQTT_PASSWORD_FILE=/opt/gadget/mosquitto/passwordfile
```

برای ساخت یک secret امن می‌توانید اجرا کنید:

```bash
openssl rand -base64 48
```

هر secret را جداگانه بسازید. secretهای واقعی را هیچ‌گاه در Git، screenshot، پیام عمومی یا فایل‌های commit‌شده قرار ندهید.

---

## 6. راه‌اندازی HTTPS برای API

فایل Nginx نمونه در [deploy/linux/gadget-api.nginx.conf](deploy/linux/gadget-api.nginx.conf) است.

### مرحله 1: جایگزین کردن domain

در فایل Nginx، `YOUR_DOMAIN` را با domain واقعی API جایگزین کنید؛ مثلاً:

```text
api.example.com
```

### مرحله 2: فعال‌کردن Nginx

```bash
sudo cp deploy/linux/gadget-api.nginx.conf /etc/nginx/sites-available/gadget-api
sudo ln -s /etc/nginx/sites-available/gadget-api /etc/nginx/sites-enabled/gadget-api
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

### مرحله 3: گرفتن certificate

```bash
sudo certbot --nginx -d api.example.com
```

Certbot certificate را می‌گیرد، تنظیم HTTPS ایجاد می‌کند و HTTP را به HTTPS منتقل می‌کند.

بعد از آن API از این آدرس در دسترس است:

```text
https://api.example.com/api/v1/health/
```

---

## 7. راه‌اندازی MQTT امن

MQTT برای پیام سریع به دستگاه استفاده می‌شود؛ مثلاً وقتی config دستگاه تغییر کرده است.

### مرحله 1: certificate برای MQTT

برای `mqtt.example.com` certificate بگیرید. روش دقیق وابسته به نحوهٔ DNS و Nginx شماست، اما در نهایت فایل‌های زیر لازم هستند:

```text
/etc/letsencrypt/live/mqtt.example.com/fullchain.pem
/etc/letsencrypt/live/mqtt.example.com/privkey.pem
/etc/letsencrypt/live/mqtt.example.com/chain.pem
```

### مرحله 2: ساخت password file

```bash
sudo mkdir -p /opt/gadget/mosquitto
sudo chown "$USER":"$USER" /opt/gadget/mosquitto

docker run --rm -it -v /opt/gadget/mosquitto:/work eclipse-mosquitto:2 \
  mosquitto_passwd -c /work/passwordfile MQTT_USER

chmod 600 /opt/gadget/mosquitto/passwordfile
```

### مرحله 3: ACL

در production بهتر است هر دستگاه فقط topic خودش را ببیند. یعنی دستگاه `ESP32-ABC123` فقط اجازهٔ خواندن این مسیر را داشته باشد:

```text
gadget/v1/devices/ESP32-ABC123/#
```

فایل نمونهٔ امن Mosquitto در [deploy/linux/mosquitto.production.conf](deploy/linux/mosquitto.production.conf) وجود دارد.

---

## 8. اجرای خود پروژه روی سرور

از ریشهٔ repository:

```bash
cd /opt/gadget/app

docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml \
  up -d --build
```

دیدن وضعیت:

```bash
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml ps
```

دیدن logها:

```bash
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml logs -f
```

اجرای migration و ساخت مدیر:

```bash
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml exec api python manage.py migrate

docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml exec api python manage.py createsuperuser
```

سپس پنل مدیریت:

```text
https://api.example.com/admin/
```

---

## 9. دستگاه جدید را چگونه اضافه کنم؟

### روش معمول: از WordPress

در WordPress دستگاه ایجاد یا ویرایش می‌شود. WordPress webhook می‌فرستد و Django آن را در دیتابیس خود می‌سازد یا به‌روزرسانی می‌کند.

دستگاه حداقل باید این اطلاعات را داشته باشد:

```json
{
  "external_id": "ESP32-ABC123",
  "serial_number": "SN-0001",
  "hardware_model": "esp32-s3-display-v1",
  "name": "Lobby Display",
  "plan": "pro",
  "is_active": true
}
```

`external_id` بسیار مهم است؛ این شناسه در API، دیتابیس و MQTT استفاده می‌شود. بعد از ساخت دستگاه آن را عوض نکنید.

### مرحلهٔ بعد: دادن token به دستگاه

دستگاه تازه هنوز اجازهٔ اتصال ندارد. باید provision شود:

```bash
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml exec api \
  python manage.py provision_device ESP32-ABC123
```

یک token روی صفحه چاپ می‌شود. این token را فقط یک بار می‌بینید. آن را در حافظهٔ امن دستگاه یا فرآیند کارخانه ذخیره کنید.

برای تعویض token:

```bash
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml exec api \
  python manage.py provision_device ESP32-ABC123 --rotate
```

بعد از rotate، token قبلی دیگر کار نمی‌کند.

---

## 10. دستگاه ESP32 چه کارهایی باید انجام دهد؟

دستگاه در حالت عادی این چرخه را دارد:

1. به Wi‑Fi وصل می‌شود.
2. با MQTT امن به `mqtt.example.com:8883` وصل می‌شود.
3. به topic اختصاصی خودش subscribe می‌کند.
4. config را از API می‌گیرد.
5. UI را render می‌کند.
6. هر 1 تا 2 دقیقه heartbeat می‌فرستد.
7. اگر پیام MQTT تنظیمات جدید آمد، دوباره config را می‌گیرد.
8. هر چند ساعت یا بعد از دستور MQTT، OTA update check انجام می‌دهد.

### هدر اجباری در درخواست دستگاه

```http
X-Device-Token: TOKEN_THAT_WAS_CREATED_DURING_PROVISIONING
```

بدون این header، server درخواست را قبول نمی‌کند.

### دریافت تنظیمات صفحه

```http
GET /api/v1/devices/ESP32-ABC123/display-config/
```

### ارسال heartbeat

```http
POST /api/v1/devices/ESP32-ABC123/heartbeat/
Content-Type: application/json
```

نمونهٔ JSON:

```json
{
  "status": "online",
  "firmware_version": "1.0.0",
  "battery_level": 80,
  "signal_strength": -58,
  "config_version": 3,
  "error_message": ""
}
```

### بررسی firmware جدید

```http
GET /api/v1/devices/ESP32-ABC123/firmware/update-check/?firmware_version=1.0.0
```

دستگاه ابتدا فایل را دانلود می‌کند، SHA-256 آن را بررسی می‌کند و فقط در صورت درست‌بودن checksum آن را نصب می‌کند.

---

## 11. config و UI را چطور تغییر بدهم؟

دو روش وجود دارد.

### روش اول: WordPress

برای تغییر اطلاعات مشتری، plan، نام دستگاه یا custom config از WordPress استفاده کنید. Django تغییر را دریافت می‌کند.

مثال custom config برای plan `pro`:

```json
{
  "theme": {
    "primary_color": "#FF9900",
    "background_color": "#111111"
  },
  "screens": [
    {"type": "clock", "enabled": true},
    {"type": "weather", "enabled": true}
  ]
}
```

### روش دوم: Django Admin

برای ساخت layoutهای حرفه‌ای وارد Admin شوید:

```text
https://api.example.com/admin/
```

بخش‌های مهم:

| بخش Admin | کاربرد |
|---|---|
| WordPress Devices | دیدن و ویرایش اطلاعات دستگاه، plan و وضعیت provisioning |
| Device Status | دیدن آنلاین/offline بودن دستگاه، باتری، Wi‑Fi و firmware |
| UI Templates | ساخت template کلی برای `simple` یا `pro` |
| UI Pages | ساخت صفحه‌های UI |
| UI Elements | ساخت متن، تصویر، نمودار یا widgetها |
| Plan Rules | محدودیت‌ها و امکانات هر plan |
| Firmware Releases | انتشار نسخهٔ جدید firmware |
| Firmware Deployments | دیدن نتیجهٔ update هر دستگاه |
| Service Health | وضعیت API، DB، Redis، MQTT و Worker |

### تغییر سادهٔ رنگ

برای plan `pro`:

1. دستگاه را در WordPress یا Admin پیدا کنید.
2. `custom_config` را تغییر دهید.
3. sync/webhook انجام می‌شود.
4. `ui_version` بالا می‌رود.
5. پیام MQTT برای دستگاه فرستاده می‌شود.
6. دستگاه config جدید را می‌گیرد.

برای `simple` بهتر است layout ثابت باقی بماند، مگر این‌که عمداً template پایهٔ simple را تغییر دهید.

---

## 12. فرمت UI چگونه است؟

UI از سه لایه تشکیل می‌شود:

```text
Template
  └── Page
        └── Element
```

مثال ساده:

```json
{
  "template": {"name": "Pro Dashboard", "version": "1.0"},
  "theme": {"name": "dark", "primary_color": "#00B7FF"},
  "pages": [
    {
      "key": "home",
      "type": "dashboard",
      "priority": 0,
      "refresh_interval_ms": 60000,
      "elements": [
        {
          "type": "text",
          "label": "Temperature",
          "source_key": "weather.temperature",
          "position": {"x": 10, "y": 20, "width": 200, "height": 40},
          "font_size": "24",
          "color": "#FFFFFF",
          "style": {},
          "config": {}
        }
      ]
    }
  ]
}
```

### اگر widget جدید بخواهم چه کنم؟

مثلاً می‌خواهید widget به نام `gauge` اضافه کنید:

1. در firmware renderer مربوط به `gauge` را بنویسید.
2. شکل JSON آن را مشخص کنید.
3. در Django Admin یک UI Element با `element_type = gauge` بسازید.
4. در `config` مقادیر اختصاصی را بدهید:

```json
{
  "min": 0,
  "max": 100,
  "unit": "%"
}
```

5. ابتدا روی دستگاه آزمایشی تست کنید.
6. بعد از درست‌بودن، آن را برای template اصلی فعال کنید.

هرگز نام فیلدهای قدیمی را ناگهان حذف نکنید. فیلد جدید را اضافه کنید تا firmware قدیمی آن را نادیده بگیرد.

---

## 13. OTA؛ چگونه firmware جدید منتشر کنم؟

1. firmware را build کنید؛ مثلاً فایل `firmware.bin`.
2. checksum آن را محاسبه کنید:

```bash
shasum -a 256 firmware.bin
```

3. فایل را در یک آدرس **HTTPS** قرار دهید.
4. در Admin به بخش **Firmware Releases** بروید.
5. این موارد را وارد کنید:
   - `hardware_model`: دقیقاً برابر مدل دستگاه؛ مثلاً `esp32-s3-display-v1`
   - `version`: مثل `1.1.0`
   - `download_url`: لینک HTTPS فایل
   - `checksum_sha256`: خروجی دستور بالا
   - `release_notes`: توضیح تغییرات
   - `is_active`: فعال
   - `is_mandatory`: اگر آپدیت اجباری است
6. ابتدا فقط یک دستگاه آزمایشی را بررسی کنید.
7. نتیجه را در **Firmware Deployments** ببینید.

اگر firmware خراب بود:

1. release خراب را در Admin غیرفعال کنید.
2. نسخهٔ پایدار قبلی را فعال کنید.
3. دستگاه‌ها در update check بعدی نسخهٔ درست را دریافت می‌کنند.

---

## 14. WordPress چگونه به سرور خبر می‌دهد؟

WordPress به این endpoint درخواست می‌فرستد:

```text
POST https://api.example.com/api/v1/integrations/wordpress/webhook/
```

Headerهای لازم:

```http
X-WP-Timestamp: UNIX_TIMESTAMP
X-WP-Signature: HMAC_SHA256_SIGNATURE
```

سرور بررسی می‌کند که:

- امضا صحیح است.
- زمان پیام خیلی قدیمی نیست.
- پیام از secret صحیح استفاده کرده است.

نمونهٔ payload:

```json
{
  "event": "device.updated",
  "entity_type": "device",
  "entity": {
    "external_id": "ESP32-ABC123",
    "name": "نمایشگر لابی",
    "hardware_model": "esp32-s3-display-v1",
    "plan": "pro",
    "is_active": true,
    "custom_config": {
      "theme": {"primary_color": "#FF9900"}
    }
  }
}
```

اگر WordPress webhook ارسال نکرد یا موقتاً مشکل داشت، pull sync هر 15 دقیقه اطلاعات را دوباره می‌خواند.

---

## 15. وضعیت سرویس‌ها و دستگاه‌ها را چطور ببینم؟

### در Admin

از Admin این بخش‌ها را ببینید:

- **Service Health**: API، Database، Redis، MQTT و Worker.
- **Device Status**: وضعیت آنلاین بودن دستگاه‌ها.
- **Firmware Deployments**: نتیجهٔ OTA.
- **WordPress Sync Events**: تاریخچهٔ syncها و خطاها.

### در API

```bash
curl https://api.example.com/api/v1/health/
```

### در سرور

```bash
# وضعیت همهٔ containerها
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml ps

# logهای همهٔ سرویس‌ها
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml logs -f

# فقط worker و beat
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml logs -f worker beat

# تست سریع وابستگی‌ها
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml exec api python manage.py check_services
```

---

## 16. اگر بخواهم تغییری در پروژه بدهم چه کنم؟

### تغییر در دیتابیس

مثلاً می‌خواهید فیلد جدیدی برای دستگاه اضافه کنید:

1. مدل را در [backend/core/models.py](backend/core/models.py) تغییر دهید.
2. migration بسازید.
3. migration را روی سرور اجرا کنید.
4. Admin و API را تغییر دهید.
5. برای آن test بنویسید.
6. مستندات JSON را به‌روزرسانی کنید.

### تغییر در API

1. منطق را در [backend/core/views.py](backend/core/views.py) اضافه یا ویرایش کنید.
2. route را در [backend/core/urls.py](backend/core/urls.py) ثبت کنید.
3. نمونهٔ JSON request/response را در مستندات اضافه کنید.
4. test بنویسید.
5. اگر تغییر breaking است، API جدید مثل `/api/v2/` بسازید؛ API قدیمی را ناگهان نشکنید.

### تغییر در WordPress Sync

1. schema WordPress را تغییر دهید.
2. webhook و pull endpoint را هماهنگ کنید.
3. منطق تبدیل داده را در [backend/core/services.py](backend/core/services.py) تغییر دهید.
4. نمونه payload جدید را test کنید.

### تغییر در UI

1. اول firmware را برای درک widget/field جدید آماده کنید.
2. سپس UI Template یا custom config را تغییر دهید.
3. ابتدا روی دستگاه test اجرا کنید.
4. بعد برای همهٔ دستگاه‌ها فعال کنید.

---

## 17. Backup و به‌روزرسانی سرور

### Backup دیتابیس

```bash
cd /opt/gadget/app
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml exec -T db \
  pg_dump -U gadget gadget > backup-$(date +%F).sql
```

این backup را خارج از همان سرور هم نگه دارید؛ مثلاً object storage یا سرور backup دیگر.

### آپدیت کردن کد سرور

```bash
cd /opt/gadget/app
git pull

docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml up -d --build

docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml exec api python manage.py migrate
```

بعد از deploy این موارد را بررسی کنید:

```bash
curl https://api.example.com/api/v1/health/
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml ps
```

---

## 18. چک‌لیست سادهٔ نهایی

قبل از وصل‌کردن دستگاه واقعی مطمئن شوید:

- [ ] domain API و domain MQTT درست تنظیم شده‌اند.
- [ ] HTTPS API فعال است.
- [ ] MQTT با TLS روی پورت 8883 فعال است.
- [ ] database و Redis public نیستند.
- [ ] `.env.production` امن است و commit نشده است.
- [ ] یک superuser ساخته‌اید.
- [ ] یک device آزمایشی از WordPress یا Admin ساخته‌اید.
- [ ] دستگاه آزمایشی را provision کرده‌اید.
- [ ] config API با `X-Device-Token` درست جواب می‌دهد.
- [ ] heartbeat در Admin نمایش داده می‌شود.
- [ ] MQTT config change را دریافت می‌کند.
- [ ] یک firmware release آزمایشی با checksum درست ساخته‌اید.
- [ ] backup دیتابیس را حداقل یک‌بار آزمایش کرده‌اید.

اگر همهٔ موارد بالا درست باشد، سرور برای شروع اتصال دستگاه‌ها آماده است.

---

## 19. اصل مهم پروژه: همه‌چیز از پنل گرافیکی

بعد از راه‌اندازی اولیهٔ Linux، هدف این است که **تو و کارمند آینده‌ات برای کارهای روزمره وارد کد، فایل JSON، `.env`، Docker یا دیتابیس نشوید.**

فایل‌ها و کد فقط برای تیم فنی هستند. کارهای روزمرهٔ کسب‌وکاری باید با دکمه، فرم، انتخاب‌گر، wizard و گزارش در پنل انجام شوند.

### پنل‌هایی که باید داشته باشیم

| پنل | استفاده‌کننده | دسترسی |
|---|---|---|
| **پنل مالک پلتفرم** | تو و مدیر ارشد | همهٔ شرکت‌ها، همهٔ دستگاه‌ها، templateها، firmware و تنظیمات مجاز |
| **پنل کارمند داخلی** | کارمند مورد اعتماد تو | فقط کارهایی که تو تعیین می‌کنی؛ مثلاً ثبت دستگاه یا بررسی خطا |
| **پنل شرکت مشتری** | مدیر شرکت | فقط دستگاه‌ها، پیام‌ها و sourceهای شرکت خودش |
| **نمایندگی فروش** | نمایندگی | در مدل تو هیچ پنل و دسترسی ندارد |

### کارهایی که باید کاملاً گرافیکی باشند

- ساخت شرکت و ساخت حساب مدیر شرکت
- افزودن دستگاه، سریال، مدل سخت‌افزار و plan
- فعال/غیرفعال یا suspend کردن دستگاه
- تولید و rotate کردن token دستگاه؛ token فقط یک‌بار در پنل نمایش داده می‌شود
- دیدن وضعیت online/offline، باتری، Wi‑Fi، خطا و firmware
- ساخت پیام تبلیغاتی و انتخاب دستگاه‌های گیرنده
- زمان‌بندی شروع و پایان نمایش پیام
- انتخاب templateهای آمادهٔ گجت
- ساخت Firmware Release و دیدن نتیجهٔ نصب آن
- افزودن API سایت از فرم
- انتخاب کانال یا گروه Telegram با wizard اتصال Bot
- دیدن گزارش، خطا و وضعیت sync

### اضافه‌کردن API خارجی بدون کدنویسی

برای کارمند یا مدیر شرکت باید یک فرم شبیه این ساخته شود:

```text
نام منبع:             آب‌وهوای تهران
نوع منبع:             API آماده / Telegram / RSS / دادهٔ داخلی
Provider:              OpenWeather
کلید نمایش:            weather.tehran
بازهٔ بروزرسانی:       هر 15 دقیقه
دستگاه‌های مجاز:       شعبه‌های شرکت A
وضعیت:                 فعال
```

برای API اختصاصی نیز wizard باید این مراحل را داشته باشد:

1. واردکردن URL و روش درخواست (`GET` یا `POST`).
2. واردکردن header یا credential در فیلد امن.
3. دریافت یک نمونه پاسخ از API.
4. نمایش درختی پاسخ JSON به کاربر.
5. انتخاب فیلدهای لازم با کلیک؛ مثلاً `temperature` و `humidity`.
6. نمایش preview خروجی استانداردی که گجت دریافت خواهد کرد.
7. ذخیره و فعال‌کردن source.

یعنی کاربر نباید Python، JSONPath پیچیده، Docker یا فایل سرور بنویسد. سیستم از انتخاب‌های فرم، mapping داخلی را می‌سازد.

### کنترل کامل UI دست تو باشد

تو page، widget، layout، رنگ‌های مجاز و نوع داده‌ها را تعریف می‌کنی. شرکت فقط فیلدهای محتوایی مجاز را تغییر می‌دهد؛ مثلاً متن تبلیغ، تصویر، لینک QR یا انتخاب یک source تأییدشده.

```text
تو:       ساختار صفحه و قابلیت‌ها
شرکت:     محتوای مجاز خودش
نمایندگی: بدون دسترسی
```

### مواردی که نباید در پنل عادی قابل تغییر باشند

- کد Python، Docker، Nginx و فایل‌های Linux
- password دیتابیس، secretهای عمومی و token دستگاه‌های دیگر
- firmware release بدون تأیید مدیر اصلی
- اطلاعات شرکت‌های دیگر
- اجرای کد JavaScript/Python یا query خطرناک در API source
- تغییر layout پایه، مگر با اجازهٔ صریح تو

### وضعیت فعلی و کار بعدی

پنل گرافیکی اولیهٔ نقش‌محور اکنون در مسیر زیر موجود است:

```text
https://api.example.com/panel/
```

این پنل پس از login، دستگاه‌ها، پیام‌ها و منابع داده را فقط در محدودهٔ شرکت مجاز نمایش می‌دهد. مالک پلتفرم و کارمند داخلی همهٔ شرکت‌ها را می‌بینند؛ مدیر شرکت فقط شرکت خودش را می‌بیند.

کارهای تکمیلی بعدی برای کامل‌ترشدن این پنل عبارت‌اند از:

1. مدل `Company` و اتصال دستگاه‌ها به شرکت.
2. نقش‌های مالک پلتفرم، کارمند داخلی و مدیر شرکت.
3. wizard پیشرفتهٔ Telegram و تست اتصال API.
4. preview گرافیکی mapping پاسخ API.
5. گزارش و ویرایش کامل پیام‌های ارسال‌شده.
6. محدودسازی تمام امکانات بر اساس plan `simple` و `pro`.

نتیجه: در استفادهٔ روزمره، تو و کارمندت فقط وارد پنل می‌شوید و هیچ نیازی به تغییر فایل یا کد ندارید.

---

## 20. مراحل نهایی deploy و تست پنل

بعد از انتقال پروژه به سرور Linux، این مراحل را به‌ترتیب انجام دهید:

```bash
cd /opt/gadget/app
git pull
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml up -d --build
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml exec api python manage.py migrate
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml exec api python manage.py createsuperuser
```

برای اجرای خودکار stack بعد از restart سرور:

```bash
sudo cp deploy/linux/gadget-platform.service /etc/systemd/system/gadget-platform.service
sudo systemctl daemon-reload
sudo systemctl enable --now gadget-platform
sudo systemctl status gadget-platform
```

### تستی که خودت از پنل انجام می‌دهی

1. وارد `https://api.example.com/panel/` شو.
2. از بخش شرکت‌ها یک شرکت آزمایشی بساز.
3. از بخش کاربران یک مدیر شرکت آزمایشی بساز.
4. از بخش دستگاه‌ها یک دستگاه بساز و model سخت‌افزار آن را وارد کن.
5. داخل جزئیات دستگاه روی تولید token بزن و token را در محل امن ذخیره کن.
6. از بخش Firmware یک فایل آزمایشی `.bin` بارگذاری کن.
7. از بخش منبع داده، یک API آزمایشی اضافه کن.
8. از بخش پیام‌ها، پیام بساز، دستگاه مقصد را انتخاب کن و زمان ارسال بده.
9. با حساب مدیر شرکت وارد شو و مطمئن شو فقط داده‌های همان شرکت را می‌بیند.
10. با حساب مالک پلتفرم وارد شو و مطمئن شو همهٔ شرکت‌ها را می‌بینی.

اگر هر بخش درست کار نکرد، logها را ببینید:

```bash
docker compose --env-file .env.production -f deploy/linux/docker-compose.production.yml logs -f api worker beat
```
