# راهنمای سادهٔ اجرای سرور Gadget

این راهنما فقط برای **بالاآوردن، بررسی و ورود به پنل سرور** است. در این مرحله لازم نیست WordPress، ESP32، منبع دادهٔ خارجی یا MQTT واقعی را وصل کنید.

دو روش دارید:

| روش | کاربرد | نیازمندی |
|---|---|---|
| **آزمایشی** | پیشنهاد برای شروع؛ بررسی پنل از مرورگر Mac | فقط سرور Linux و SSH |
| **واقعی** | استفادهٔ دائمی و اتصال بعدی دستگاه‌ها از اینترنت | domain، HTTPS و تنظیم MQTT امن |

> ابتدا روش **آزمایشی** را انجام دهید. وقتی پنل را دیدید و کارکرد آن تأیید شد، سرور را با روش واقعی مستقر کنید.

---

## 1. تصویر سادهٔ سیستم

```text
Mac شخصی شما                         سرور Linux
────────────                         ───────────
مرورگر ──► SSH Tunnel امن ────────► پنل Django
Terminal ─► SSH مدیریت ───────────► Docker + PostgreSQL + Redis
```

در حالت آزمایشی، هیچ پورت پنل یا دیتابیس برای اینترنت عمومی باز نمی‌شود. فقط خود شما، از طریق SSH، پنل را در مرورگر Mac می‌بینید.

---

# بخش A — اجرای آزمایشی و ساده (پیشنهاد شروع)

## 2. چیزهایی که لازم دارید

- یک سرور Ubuntu 22.04 یا 24.04 با حداقل 2 CPU و 4GB RAM
- IP سرور، نام کاربر SSH و رمز یا SSH Key آن
- Mac شخصی با Terminal
- دسترسی به repository پروژه در GitHub

در این حالت **لازم نیست** domain، Nginx، SSL، WordPress یا دستگاه واقعی داشته باشید.

## 3. ورود به سرور و نصب Docker

روی Mac، برنامهٔ **Terminal** را باز کنید و به سرور وصل شوید:

````bash
ssh SERVER_USER@SERVER_IP
````

مثال:

````bash
ssh root@203.0.113.10
````

سپس روی سرور این دستورها را اجرا کنید:

````bash
sudo apt update
sudo apt install -y ca-certificates curl git openssl
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker "$USER"
exit
````

دوباره از Mac به سرور وصل شوید و بررسی کنید Docker آماده است:

````bash
ssh SERVER_USER@SERVER_IP
docker --version
docker compose version
````

## 4. دریافت پروژه از GitHub

اگر repository شما private است، ابتدا روی سرور SSH key بسازید:

````bash
ssh-keygen -t ed25519 -C "gadget-linux-server"
cat ~/.ssh/id_ed25519.pub
````

خروجی دستور دوم را در GitHub اضافه کنید:

```text
GitHub → Settings → SSH and GPG keys → New SSH key
```

سپس اتصال را بررسی و پروژه را دریافت کنید:

````bash
ssh -T git@github.com
sudo mkdir -p /opt/gadget
sudo chown -R "$USER":"$USER" /opt/gadget
git clone git@github.com:VahidMB/New-Gadget-Project.git /opt/gadget/app
cd /opt/gadget/app
````

اگر قبلاً clone ناقص داشته‌اید، ابتدا آن را پاک کنید:

````bash
rm -rf /opt/gadget/app
````

و سپس دوباره دستور `git clone` را اجرا کنید.

## 5. ساخت تنظیمات تست

در مسیر پروژه، فایل تنظیمات تست را بسازید:

````bash
cd /opt/gadget/app
cp .env.test.example .env.test
chmod 600 .env.test
nano .env.test
````

فقط این سه مقدار نمونه را با مقدار واقعی عوض کنید:

````dotenv
DJANGO_SECRET_KEY=یک-secret-تصادفی-طولانی
POSTGRES_PASSWORD=یک-رمز-قوی
WORDPRESS_WEBHOOK_SECRET=یک-secret-تصادفی-دیگر
````

برای ساخت هر مقدار تصادفی اجرا کنید:

````bash
openssl rand -hex 32
````

بقیهٔ فایل را تغییر ندهید. به‌خصوص این سه مقدار باید برای تست همین‌طور بمانند:

````dotenv
DJANGO_SECURE_SSL_REDIRECT=0
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
MQTT_USE_TLS=0
````

در `nano` ذخیره کنید: `Ctrl + O`، سپس `Enter` و در پایان `Ctrl + X`.

## 6. اجرای سرور آزمایشی

روی سرور اجرا کنید:

````bash
cd /opt/gadget/app
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  up -d --build
````

بار اول چند دقیقه زمان می‌برد. بعد وضعیت را ببینید:

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml ps
````

اگر `api` به حالت `healthy` رسید، جدول‌های دیتابیس و حساب مدیر را بسازید:

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  exec api python manage.py migrate

docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  exec api python manage.py createsuperuser
````

> اگر API با خطای `readonly_fields` اجرا نشد، ابتدا آخرین کد پروژه را دریافت کنید (`git pull`) و دوباره دستور `up -d --build` را اجرا کنید. این خطا در کد فعلی اصلاح شده است.

## 7. تست سلامت روی خود سرور

روی Linux اجرا کنید:

````bash
curl http://127.0.0.1:8000/api/v1/health/
````

باید پاسخ JSON بگیرید. اگر خطا دیدید، لاگ API را ببینید:

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  logs --tail=100 api
````

## 8. ورود از مرورگر Mac به پنل

روی Mac یک **Terminal جدید** باز کنید؛ این دستور را روی خود Mac اجرا کنید، نه داخل SSH سرور:

````bash
ssh -N -L 8000:127.0.0.1:8000 SERVER_USER@SERVER_IP
````

این Terminal را باز نگه دارید. سپس در مرورگر Mac باز کنید:

```text
http://127.0.0.1:8000/panel/
```

پنل فنی Django Admin نیز:

```text
http://127.0.0.1:8000/admin/
```

با اطلاعاتی که در `createsuperuser` وارد کردید login کنید.

اگر پورت 8000 روی Mac اشغال است، tunnel را با 8080 باز کنید:

````bash
ssh -N -L 8080:127.0.0.1:8000 SERVER_USER@SERVER_IP
````

و در مرورگر از این آدرس استفاده کنید:

```text
http://127.0.0.1:8080/panel/
```

## 9. تست پنل بدون اتصال هیچ سرویس خارجی

برای اطمینان از آماده‌بودن پنل، این کارها را انجام دهید:

1. وارد `/panel/` شوید.
2. یک شرکت آزمایشی بسازید.
3. یک دستگاه آزمایشی با plan `simple` بسازید.
4. یک دستگاه آزمایشی دیگر با plan `pro` بسازید.
5. یک پیام آزمایشی ایجاد کنید.
6. یک منبع دادهٔ آزمایشی از نوع `HTTP API` بسازید؛ لازم نیست URL واقعی بدهید.
7. بخش Firmware را باز کنید و فرم upload را بررسی کنید.

در این مرحله نبودن WordPress، ESP32، API بیرونی و Telegram طبیعی است و مانع تست پنل نمی‌شود.

## 10. دستورهای روزمرهٔ محیط تست

وضعیت سرویس‌ها:

````bash
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml ps
````

لاگ API:

````bash
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml logs -f api
````

توقف سرویس‌ها:

````bash
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml down
````

حذف کامل اطلاعات تستی (فقط در صورت نیاز):

````bash
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml down -v
````

---

# بخش B — اجرای واقعی و دائمی

این بخش زمانی است که می‌خواهید کاربران یا دستگاه‌های واقعی از اینترنت به سرور وصل شوند.

## 11. تفاوت اجرای واقعی با تست

| مورد | آزمایشی | واقعی |
|---|---|---|
| ورود شما به پنل | SSH Tunnel | HTTPS با domain |
| API عمومی | خیر | فقط از طریق HTTPS |
| MQTT دستگاه | داخلی و آزمایشی | TLS روی پورت 8883 |
| فایل تنظیمات | `.env.test` | `.env.production` |
| compose | `docker-compose.test.yml` | `docker-compose.production.yml` |

## 12. نیازهای اجرای واقعی

- domain برای API، مثل `api.example.com`
- DNS domain که به IP سرور اشاره کند
- پورت‌های عمومی `80` و `443`
- اگر دستگاه واقعی دارید: پورت `8883` برای MQTT امن
- Nginx و گواهی رایگان Let's Encrypt

## 13. آماده‌سازی تنظیمات واقعی

روی سرور:

````bash
cd /opt/gadget/app
cp .env.example .env.production
chmod 600 .env.production
nano .env.production
````

حداقل این مقدارها را تنظیم کنید:

````dotenv
DJANGO_DEBUG=0
DJANGO_SECURE_SSL_REDIRECT=1
DJANGO_ALLOWED_HOSTS=api.example.com
DJANGO_SECRET_KEY=یک-secret-تصادفی-جدید
POSTGRES_PASSWORD=یک-رمز-قوی
MQTT_USE_TLS=1
MQTT_PORT=8883
````

به‌جای `api.example.com` domain واقعی خودتان را بنویسید.

تا زمانی که WordPress یا دستگاه واقعی وصل نکرده‌اید، مقادیر WordPress را خالی بگذارید. اجرای پنل به WordPress وابسته نیست.

## 14. اجرای سرویس‌های واقعی

````bash
cd /opt/gadget/app
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml \
  up -d --build

docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml \
  exec api python manage.py migrate

docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml \
  exec api python manage.py createsuperuser
````

## 15. فعال‌کردن HTTPS و domain

برای این مرحله از فایل نمونهٔ Nginx استفاده کنید:

```text
deploy/linux/gadget-api.nginx.conf
```

`YOUR_DOMAIN` را در آن با domain واقعی جایگزین کنید. سپس Nginx و Certbot را روی Ubuntu نصب و certificate را بگیرید:

````bash
sudo apt install -y nginx certbot python3-certbot-nginx
sudo cp deploy/linux/gadget-api.nginx.conf /etc/nginx/sites-available/gadget-api
sudo ln -s /etc/nginx/sites-available/gadget-api /etc/nginx/sites-enabled/gadget-api
sudo nginx -t
sudo systemctl reload nginx
sudo certbot --nginx -d api.example.com
````

پس از آن پنل با این آدرس باز می‌شود:

```text
https://api.example.com/panel/
```

## 16. نگه‌داری سادهٔ سرور واقعی

برای به‌روزرسانی کد:

````bash
cd /opt/gadget/app
git pull
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml \
  up -d --build
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml \
  exec api python manage.py migrate
````

برای مشاهدهٔ لاگ API:

````bash
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml logs -f api
````

---

## 17. نتیجهٔ نهایی

برای شروع، فقط بخش A را انجام دهید:

```text
Linux Server → Docker → SSH Tunnel → مرورگر Mac → /panel/
```

وقتی پنل را با موفقیت دیدید، بخش B را فقط زمانی انجام دهید که domain و دستگاه‌های واقعی آماده‌اند.

راهنماهای جزئی‌تر در صورت نیاز:

- [راهنمای تست سرور و مرورگر](راهنمای-تست-سرور-و-مرورگر.md)
- [راهنمای آپدیت فریمور](راهنمای-آپدیت-فریمور.md)
- [مستندات فنی کامل](docs/)
