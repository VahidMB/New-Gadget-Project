# راهنمای سادهٔ آپدیت نرم‌افزار گجت از پنل

این راهنما توضیح می‌دهد چگونه بدون دست‌زدن به کد سرور، از پنل گرافیکی نسخهٔ جدید firmware را برای گجت‌های ESP32 بارگذاری و منتشر کنید.

> نکتهٔ مهم: در پنل فایل کد Arduino، فایل `.ino`، فایل `.cpp` یا ZIP پروژه را آپلود نمی‌کنید. فایل موردنیاز دستگاه، خروجی کامپایل‌شده با پسوند **`.bin`** است.

---

## 1. قبل از شروع چه چیزهایی لازم است؟

- سرور و containerهای پروژه در حال اجرا باشند.
- migrationهای جدید اعمال شده باشند.
- شما با حساب مالک پلتفرم یا کارمند داخلی وارد شده باشید.
- دستگاه موردنظر `hardware_model` درست داشته باشد.
- firmware جدید را قبلاً برای همان مدل سخت‌افزار build کرده باشید.

برای اطمینان از اجرای سرویس‌ها روی Linux:

```bash
cd /opt/gadget/app
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml ps
```

اگر migration جدید وجود دارد:

```bash
docker compose --env-file .env.production \
  -f deploy/linux/docker-compose.production.yml exec api python manage.py migrate
```

---

## 2. فایل درست برای آپلود چیست؟

فایل درست معمولاً شبیه یکی از این‌ها است:

```text
firmware.bin
esp32-s3-display-v1-1.2.0.bin
```

فایل‌های نادرست برای upload:

```text
main.ino
main.cpp
project.zip
source-code.zip
platformio.ini
```

پنل فقط فایل با پسوند `.bin` را قبول می‌کند.

---

## 3. ورود به پنل

در مرورگر این آدرس را باز کنید:

```text
https://api.example.com/panel/
```

با حساب مالک پلتفرم یا کارمند داخلی وارد شوید.

از منوی بالای پنل، گزینهٔ **Firmware** را انتخاب کنید.

> مدیر شرکت و نمایندگی به بخش Firmware دسترسی ندارند.

---

## 4. بارگذاری نسخهٔ جدید

1. در صفحهٔ Firmware روی دکمهٔ **بارگذاری Firmware جدید** بزنید.
2. فیلدها را پر کنید.
3. روی **بارگذاری و ثبت نسخه** بزنید.

### معنی هر فیلد

| فیلد | چه چیزی وارد کنم؟ |
|---|---|
| مدل سخت‌افزار | دقیقاً همان مدل ذخیره‌شده روی دستگاه؛ مثلاً `esp32-s3-display-v1` |
| نسخه | نسخهٔ جدید؛ مثل `1.2.0` |
| فایل Firmware | فایل کامپایل‌شدهٔ `.bin` |
| توضیحات نسخه | مثلاً «رفع مشکل Wi‑Fi و اضافه‌شدن صفحهٔ جدید» |
| اجباری | اگر روشن باشد، firmware آن را update مهم تلقی می‌کند |
| فعال | اگر روشن باشد، این نسخه در update check به دستگاه‌های همان مدل معرفی می‌شود |
| زمان انتشار | زمان انتشار نسخه؛ می‌تواند خالی باشد |

پس از upload، سرور SHA-256 فایل را **خودکار** محاسبه و ذخیره می‌کند. لازم نیست checksum را دستی وارد کنید.

---

## 5. دستگاه چگونه firmware را دریافت می‌کند؟

چرخهٔ کار به این شکل است:

```text
تو فایل .bin را در پنل upload می‌کنی
           ↓
سرور release جدید را ثبت می‌کند
           ↓
ESP32 در update check بعدی نسخهٔ جدید را می‌بیند
           ↓
فایل را از HTTPS دانلود می‌کند
           ↓
SHA-256 فایل را با مقدار سرور مقایسه می‌کند
           ↓
اگر درست بود نصب و restart می‌شود
           ↓
نتیجه را به سرور گزارش می‌دهد
```

دستگاه برای بررسی update این endpoint را با token خودش صدا می‌زند:

```text
GET /api/v1/devices/{external_id}/firmware/update-check/?firmware_version=1.1.0
```

سرور فقط releaseهایی را برمی‌گرداند که:

- `hardware_model` آن‌ها با مدل دستگاه یکی باشد.
- فعال باشند.
- نسخه‌شان با نسخهٔ فعلی دستگاه متفاوت باشد.

---

## 6. اول روی دستگاه آزمایشی تست کنید

هیچ‌وقت فایل جدید را مستقیم برای همهٔ دستگاه‌ها منتشر نکنید.

روش امن:

1. یک دستگاه تست با مدل سخت‌افزاری مشابه داشته باشید.
2. release را upload کنید.
3. ابتدا فقط همان دستگاه تست را برای update بررسی کنید.
4. در پنل Admin یا بخش وضعیت دستگاه، firmware version جدید را ببینید.
5. اگر نصب، نمایشگر، Wi‑Fi و heartbeat درست بود، نسخه را برای دستگاه‌های واقعی فعال نگه دارید.

### چه چیزی را بررسی کنم؟

- دستگاه بعد از نصب دوباره آنلاین شد.
- `firmware_version` جدید را در heartbeat گزارش کرد.
- صفحهٔ گجت درست render می‌شود.
- Wi‑Fi و MQTT وصل هستند.
- battery و signal strength گزارش می‌شوند.
- در بخش `Firmware Deployments` وضعیت `installed` دیده می‌شود.

---

## 7. اگر update خراب شد چه کنم؟

اگر نسخهٔ جدید مشکل داشت:

1. وارد پنل Firmware شوید.
2. release مشکل‌دار را غیرفعال کنید.
3. نسخهٔ پایدار قبلی را فعال کنید.
4. دلیل خطا را در `Firmware Deployments` بررسی کنید.
5. اگر دستگاه گزارش خطا داده، `error_message` آن را ببینید.

نمونه خطاهای رایج:

| خطا | معنی احتمالی |
|---|---|
| `SHA-256 mismatch` | فایل دانلودشده با فایل ثبت‌شده یکی نیست یا فایل خراب شده است |
| `Firmware release not found` | مدل سخت‌افزار یا نسخه اشتباه است |
| `Download failed` | لینک فایل، اینترنت یا certificate مشکل دارد |
| `Insufficient space` | فضای flash دستگاه کافی نیست |
| دستگاه offline شد | firmware مشکل دارد یا دستگاه restart نشده است |

---

## 8. اگر دکمهٔ Firmware را نمی‌بینم

دکمهٔ Firmware فقط برای نقش‌های زیر دیده می‌شود:

- Superuser
- مالک پلتفرم
- کارمند داخلی دارای دسترسی platform

مدیر شرکت، اپراتور شرکت و نمایندگی نباید firmware منتشر کنند.

اگر باید به یک کارمند داخلی دسترسی بدهید، در Django Admin برای او یک `Company Membership` با role برابر `employee` بسازید و حساب Django او را فعال کنید.

---

## 9. اگر فایل آپلود می‌شود ولی دستگاه دانلود نمی‌کند

این موارد را بررسی کنید:

1. آدرس API با HTTPS کار می‌کند.
2. مسیر `/media/` روی Nginx قابل دسترسی است.
3. فایل روی سرور در مسیر زیر ساخته شده است:

```text
/opt/gadget/app/data/media/firmware/
```

4. container API این مسیر را mount کرده است.
5. `hardware_model` دستگاه و release دقیقاً یکسان است.
6. release فعال است.
7. token دستگاه درست است.
8. دستگاه اینترنت و DNS دارد.
9. firmware دستگاه قابلیت OTA دارد.

تست دستی لینک فایل:

```bash
curl -I https://api.example.com/media/firmware/YEAR/MONTH/FILENAME.bin
```

باید پاسخ `200` یا `302` معتبر بگیرید، نه `404` یا `403`.

---

## 10. نکات امنیتی مهم

- فقط فایل firmware تولیدشده توسط تیم خودتان را upload کنید.
- فایل `.bin` را قبل از upload روی دستگاه تست امتحان کنید.
- releaseهای قدیمی را حذف نکنید؛ برای rollback نگه دارید.
- دسترسی upload را فقط به مالک و کارمند داخلی بدهید.
- directory فایل‌ها و پنل API باید HTTPS باشد.
- checksum خودکار مفید است، اما در آینده بهتر است firmware با کلید خصوصی نیز cryptographically sign شود.
- token دستگاه را در پنل عمومی یا پیام گروهی قرار ندهید.

---

## خلاصهٔ خیلی کوتاه

```text
کد ESP32 را build کن
      ↓
فایل .bin بگیر
      ↓
وارد /panel/ شو
      ↓
Firmware → بارگذاری Firmware جدید
      ↓
مدل سخت‌افزار و نسخه را وارد کن
      ↓
فایل .bin را انتخاب کن
      ↓
ذخیره کن
      ↓
اول با دستگاه تست امتحان کن
      ↓
وضعیت نصب را در پنل بررسی کن
```
