# راهنمای تست سرور Linux از مرورگر Mac

این راهنما برای **تست اولیهٔ پنل** است؛ بدون domain، بدون SSL، بدون WordPress و بدون دستگاه ESP32 واقعی.

در این روش:

- پروژه و Docker روی **سرور Linux** اجرا می‌شوند.
- شما با **Mac خودتان** به سرور SSH می‌زنید.
- پنل را در Safari، Chrome یا Firefox **روی Mac خودتان** باز می‌کنید.
- API، PostgreSQL، Redis و MQTT برای اینترنت عمومی باز نمی‌شوند.

> این راهنما برای آزمایش پنل است. برای اتصال دستگاه‌های واقعی از اینترنت، بعداً باید استقرار production با domain، HTTPS و MQTT TLS انجام شود.

---

## 1. تصویر سادهٔ ارتباط

```text
Mac شما                                      سرور Linux
────────                                     ───────────
Terminal اول: SSH برای مدیریت  ───────────►  اجرای Docker و سرویس‌ها
Terminal دوم: SSH Tunnel       ───────────►  127.0.0.1:8000 داخل سرور
Browser: http://127.0.0.1:8000/panel/  ◄──  Django داخل Docker
```

آدرسی که در مرورگر Mac باز می‌کنید همیشه یکی از این‌ها است:

```text
http://127.0.0.1:8000/panel/   ← پنل اصلی پروژه
http://127.0.0.1:8000/admin/   ← پنل فنی Django Admin
```

این `127.0.0.1` مربوط به **خود Mac شما** است. SSH Tunnel درخواست را به شکل رمزنگاری‌شده به سرور Linux می‌رساند.

---

## 2. اطلاعاتی که قبل از شروع لازم دارید

این موارد را داشته باشید:

- IP سرور Linux؛ مثل `203.0.113.10`
- نام کاربر SSH سرور؛ معمولاً `root` یا یک کاربر مانند `ubuntu`
- رمز SSH یا ترجیحاً SSH key
- دسترسی به repository گیت‌هاب پروژه
- Mac با Terminal استاندارد

در مثال‌ها این دو مقدار را جایگزین کنید:

```text
SERVER_USER  → نام کاربر SSH، مثل root یا ubuntu
SERVER_IP    → IP واقعی سرور، مثل 203.0.113.10
```

---

## 3. اتصال اولیه از Mac به سرور

روی Mac، برنامهٔ **Terminal** را باز کنید و اجرا کنید:

````bash
ssh SERVER_USER@SERVER_IP
````

مثال:

````bash
ssh root@203.0.113.10
````

بار اول ممکن است این سؤال را ببینید:

```text
Are you sure you want to continue connecting?
```

بنویسید:

```text
yes
```

سپس رمز سرور را وارد کنید. بعد از ورود، prompt ترمینال نشان می‌دهد که اکنون دستورها روی Linux اجرا می‌شوند.

> این پنجره را برای کارهای سرور نگه دارید. در ادامه یک Terminal دوم روی Mac برای tunnel باز می‌کنید.

---

## 4. آماده‌سازی اولیهٔ سرور Linux

دستورهای این بخش را در همان Terminal که به سرور وصل هستید اجرا کنید.

### 4.1 به‌روزرسانی و نصب ابزارهای پایه

````bash
sudo apt update
sudo apt -y upgrade
sudo apt install -y ca-certificates curl git openssl
````

### 4.2 نصب Docker

````bash
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker "$USER"
exit
````

دستور `exit` اتصال SSH را می‌بندد تا عضویت Docker برای کاربر فعال شود.

حالا در Terminal Mac دوباره وصل شوید:

````bash
ssh SERVER_USER@SERVER_IP
````

و نصب را بررسی کنید:

````bash
docker --version
docker compose version
````

هر دو دستور باید شمارهٔ نسخه نمایش دهند.

### 4.3 firewall در حالت تست

در حالت تست فقط SSH را باز نگه دارید. API و دیتابیس نباید عمومی باشند.

اگر UFW روی سرور دارید یا می‌خواهید فعالش کنید:

````bash
sudo apt install -y ufw
sudo ufw allow OpenSSH
sudo ufw enable
sudo ufw status
````

باید پورت `22` مجاز باشد. برای این تست، پورت‌های `8000`، `5432`، `6379`، `1883` و `8883` را باز نکنید.

---

## 5. دریافت پروژه از GitHub

### حالت A: repository عمومی است

````bash
sudo mkdir -p /opt/gadget
sudo chown -R "$USER":"$USER" /opt/gadget
cd /opt/gadget
git clone https://github.com/VahidMB/New-Gadget-Project.git app
cd /opt/gadget/app
````

### حالت B: repository خصوصی است — روش پیشنهادی SSH key

اگر با خطای `Invalid username or token` روبه‌رو شدید، از password گیت‌هاب استفاده نکنید. GitHub برای Git password را قبول نمی‌کند.

روی **سرور Linux** کلید بسازید:

````bash
ssh-keygen -t ed25519 -C "gadget-linux-server"
````

برای سؤال مسیر و passphrase، در حالت ساده Enter بزنید.

کلید public را ببینید و کامل کپی کنید:

````bash
cat ~/.ssh/id_ed25519.pub
````

در GitHub این مسیر را بروید:

```text
GitHub → Settings → SSH and GPG keys → New SSH key
```

کلید را paste و ذخیره کنید. سپس روی سرور اتصال را امتحان کنید:

````bash
ssh -T git@github.com
````

اگر پیام موفقیت دیدید، clone را با SSH انجام دهید:

````bash
sudo mkdir -p /opt/gadget
sudo chown -R "$USER":"$USER" /opt/gadget
cd /opt/gadget
git clone git@github.com:VahidMB/New-Gadget-Project.git app
cd /opt/gadget/app
````

اگر پیش از این clone ناقص ساخته شده است:

````bash
rm -rf /opt/gadget/app
````

سپس دستور `git clone` را دوباره اجرا کنید.

---

## 6. ساخت تنظیمات مخصوص تست

این پروژه برای تست server-browser یک فایل جدا دارد تا با تنظیمات production یا WordPress قاطی نشود.

روی سرور Linux و در مسیر `/opt/gadget/app` اجرا کنید:

````bash
cp .env.test.example .env.test
chmod 600 .env.test
nano .env.test
````

در فایل بازشده، حداقل این سه مقدار را با مقدارهای امن جایگزین کنید:

```dotenv
DJANGO_SECRET_KEY=یک-secret-تصادفی-طولانی
POSTGRES_PASSWORD=یک-رمز-قوی
WORDPRESS_WEBHOOK_SECRET=یک-secret-تصادفی-دیگر
```

برای ساخت هر مقدار تصادفی، در یک Terminal Linux اجرا کنید:

````bash
openssl rand -base64 48
````

هر بار اجرای دستور یک مقدار جدید می‌دهد. آن را کپی و جای مقدار مربوطه قرار دهید.

این مقادیر را **همان‌طور نگه دارید** تا پنل در مرورگر Mac بدون domain باز شود:

```dotenv
DJANGO_DEBUG=0
DJANGO_SECURE_SSL_REDIRECT=0
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
```

در پایان ذخیره کنید:

```text
Ctrl + O  →  Enter  →  Ctrl + X
```

در این تست عمداً WordPress غیرفعال است. `WORDPRESS_API_BASE_URL` و `WORDPRESS_API_TOKEN` را خالی بگذارید.

---

## 7. اجرای سرویس‌ها روی سرور

در سرور Linux و در مسیر پروژه اجرا کنید:

````bash
cd /opt/gadget/app

docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  up -d --build
````

بار اول ممکن است چند دقیقه طول بکشد؛ Docker imageها و وابستگی‌ها را دانلود و backend را build می‌کند.

بعد وضعیت را ببینید:

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  ps
````

سرویس‌های زیر باید در حال اجرا باشند:

```text
api
worker
beat
db
redis
mqtt
```

> API به‌صورت خودکار migration دیتابیس را هنگام start اجرا می‌کند. بااین‌حال دستور migration دستی زیر نیز امن است و برای اطمینان می‌توانید اجرا کنید:

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  exec api python manage.py migrate
````

### ساخت حساب ورود پنل

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  exec api python manage.py createsuperuser
````

سؤال‌های زیر را پر کنید:

- Username: یک نام ساده؛ مثل `admin`
- Email address: ایمیل خودتان
- Password: رمز قوی برای ورود به پنل
- Password (again): همان رمز دوباره

این username و password را نگه دارید؛ در مرورگر Mac با آن وارد می‌شوید.

### تست سلامت روی خود سرور

````bash
curl http://127.0.0.1:8000/api/v1/health/
````

باید یک پاسخ JSON ببینید. اگر خطا داشتید، به بخش عیب‌یابی بروید.

---

## 8. ساخت SSH Tunnel روی Mac

حالا یک **Terminal جدید روی Mac** باز کنید. این Terminal نباید همان پنجره‌ای باشد که در آن دستورهای Linux را وارد می‌کنید.

روی Mac اجرا کنید:

````bash
ssh -N -L 8000:127.0.0.1:8000 SERVER_USER@SERVER_IP
````

مثال:

````bash
ssh -N -L 8000:127.0.0.1:8000 root@203.0.113.10
````

بعد از واردکردن رمز یا استفاده از SSH key، معمولاً چیزی نمایش داده نمی‌شود؛ این طبیعی است. پنجره به‌ظاهر منتظر می‌ماند، ولی tunnel فعال است.

### قانون مهم

- این پنجره را **نبندید**.
- تا وقتی این دستور در حال اجرا است، پنل از مرورگر Mac باز می‌شود.
- برای قطع tunnel در همین پنجره `Control + C` بزنید.

### اگر Mac گفت پورت 8000 اشغال است

از پورت محلی 8080 استفاده کنید:

````bash
ssh -N -L 8080:127.0.0.1:8000 SERVER_USER@SERVER_IP
````

در ادامه، به‌جای `8000` در آدرس مرورگر، `8080` بزنید.

---

## 9. ورود به پنل از مرورگر Mac

بعد از فعال‌شدن tunnel، مرورگر Mac را باز کنید.

### پنل اصلی پروژه

```text
http://127.0.0.1:8000/panel/
```

### پنل فنی Django Admin

```text
http://127.0.0.1:8000/admin/
```

اگر tunnel را با پورت 8080 ساخته‌اید:

```text
http://127.0.0.1:8080/panel/
http://127.0.0.1:8080/admin/
```

با username و password ساخته‌شده در مرحلهٔ `createsuperuser` وارد شوید.

> آدرس `http://SERVER_IP:8000` را باز نکنید. API عمداً فقط روی loopback سرور قرار دارد و تنها از طریق SSH Tunnel باید به آن برسید.

---

## 10. چه چیزهایی را در پنل تست کنم؟

بعد از ورود به `/panel/` این تست‌ها را انجام دهید:

1. داشبورد را ببینید.
2. یک شرکت آزمایشی ایجاد کنید.
3. یک کاربر آزمایشی بسازید و نقش مناسب بدهید.
4. یک دستگاه آزمایشی با plan `simple` ایجاد کنید.
5. یک دستگاه آزمایشی دیگر با plan `pro` ایجاد کنید.
6. وضعیت دستگاه و جزئیات آن را باز کنید.
7. یک پیام آزمایشی بسازید و دستگاه مقصد را انتخاب کنید.
8. یک منبع دادهٔ آزمایشی ایجاد کنید؛ لازم نیست هنوز API واقعی به آن وصل باشد.
9. فرم Firmware را باز کنید و ظاهر/مراحل upload را بررسی کنید؛ فایل واقعی firmware لازم نیست.
10. از حساب admin خارج شوید و دوباره وارد شوید.

برای مدیریت فنی‌تر، `/admin/` را باز کنید. در این قسمت مدل‌ها، داده‌ها و تنظیمات Django نمایش داده می‌شوند.

---

## 11. دیدن log و پیدا کردن خطا

همهٔ logها در سرور Linux:

````bash
cd /opt/gadget/app

docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  logs -f
````

فقط log API:

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  logs -f api
````

فقط worker:

````bash
docker compose --env-file .env.test \
  -f deploy/linux/docker-compose.test.yml \
  logs -f worker
````

برای خارج‌شدن از نمایش log، `Control + C` بزنید. سرویس‌ها متوقف نمی‌شوند؛ فقط نمایش log بسته می‌شود.

---

## 12. عیب‌یابی سریع

### مرورگر Mac باز نمی‌شود یا Connection refused می‌دهد

1. مطمئن شوید Terminal tunnel روی Mac باز است.
2. روی سرور اجرا کنید:

````bash
curl http://127.0.0.1:8000/api/v1/health/
````

3. اگر پاسخ نگرفتید، وضعیت API را بررسی کنید:

````bash
cd /opt/gadget/app
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml ps
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml logs --tail=100 api
````

### خطای DisallowedHost در مرورگر

روی سرور فایل `.env.test` را باز کنید:

````bash
cd /opt/gadget/app
nano .env.test
````

مطمئن شوید این مقدار دقیقاً وجود دارد:

```dotenv
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
```

بعد سرویس را دوباره بسازید:

````bash
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml up -d --force-recreate api
````

### دستور SSH Tunnel می‌گوید Address already in use

پورت 8000 روی Mac اشغال است. tunnel را با 8080 بسازید:

````bash
ssh -N -L 8080:127.0.0.1:8000 SERVER_USER@SERVER_IP
````

و در مرورگر باز کنید:

```text
http://127.0.0.1:8080/panel/
```

### Docker سرویس‌ها را بالا نمی‌آورد

روی سرور بررسی کنید:

````bash
docker ps
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml logs --tail=200
````

همچنین مطمئن شوید داخل `/opt/gadget/app` هستید و فایل `.env.test` واقعاً وجود دارد:

````bash
ls -la .env.test deploy/linux/docker-compose.test.yml
````

---

## 13. توقف، شروع دوباره و پاک‌کردن محیط تست

### توقف موقت سرویس‌ها

روی سرور Linux:

````bash
cd /opt/gadget/app
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml stop
````

### شروع دوباره

````bash
cd /opt/gadget/app
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml start
````

### توقف و حذف containerها؛ داده‌ها حفظ می‌شوند

````bash
cd /opt/gadget/app
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml down
````

### پاک‌کردن کامل داده‌های تست

> هشدار: دستور زیر تمام شرکت‌ها، کاربران، دستگاه‌ها و داده‌های تستی را حذف می‌کند.

````bash
cd /opt/gadget/app
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml down -v
````

بعد از پاک‌سازی، دوباره از مرحلهٔ 7 شروع کنید و یک superuser جدید بسازید.

---

## 14. خلاصهٔ خیلی کوتاه برای هر بار استفاده

### روی سرور Linux

````bash
cd /opt/gadget/app
docker compose --env-file .env.test -f deploy/linux/docker-compose.test.yml up -d
````

### روی Mac؛ در یک Terminal جدا

````bash
ssh -N -L 8000:127.0.0.1:8000 SERVER_USER@SERVER_IP
````

### در مرورگر Mac

```text
http://127.0.0.1:8000/panel/
```

تمام. تا وقتی tunnel باز است، پنل سرور را امن و مستقیم از مرورگر سیستم خودتان می‌بینید.
